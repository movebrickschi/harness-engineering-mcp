# 02 方案设计

- **任务 ID**：T-XXXX
- **状态**：Draft / Approved / Outdated
- **更新时间**：YYYY-MM-DD
- **负责人**：<姓名>

## 1. 总体方案

简述：选择的技术路线、关键组件、模块划分。必要时给出架构图。

```mermaid
flowchart LR
  ...
```

## 2. 模块边界

| 模块 | 职责 | 输入 | 输出 |
| --- | --- | --- | --- |
| … | … | … | … |

## 3. API 契约

| 接口 | 方法 | 鉴权 | 请求 | 返回 | 错误 |
| --- | --- | --- | --- | --- | --- |
| /api/admin/xxx | GET | 后台用户 | 见下 | R<XxxVO> | 400/401/403/404 |

请求字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| … | … | … | … |

返回结构：

```json
{ "code": 0, "message": "ok", "data": { } }
```

## 4. 数据库变更

- 涉及表：
- 新增字段 / 索引 / 表：
- Flyway 脚本：`V<n>__<desc>.sql`
- 回滚 SQL：

## 5. 配置 / 环境

- 新增 Nacos 配置：
- 新增环境变量：
- 默认值与生效范围：

## 6. 缓存 / 搜索 / 异步 / 锁

- 缓存 key 与 TTL：
- ES 索引 / 字段：
- 异步任务 / 消息队列：
- 分布式锁场景与超时：

## 7. 第三方依赖

- SDK / 接口：
- 重试策略 / 超时：
- 失败回放剧本：

## 8. 安全设计

- 输入校验 / 输出转义
- 权限校验位置（@PreAuthorize / Service 内）
- 敏感信息脱敏 / 不入日志

## 9. 性能与容量评估

- 预期 QPS / 数据量
- 关键 SQL 走的索引
- 是否需要分页 / 缓存 / 限流

## 10. 兼容性

- 是否影响已有调用方
- 灰度策略 / 切换方案
- 回滚方案

## 11. 风险与替代方案

| 风险 | 概率 | 影响 | 缓解措施 |
| --- | --- | --- | --- |

| 替代方案 | 优点 | 缺点 | 拒绝原因 |
| --- | --- | --- | --- |

## 12. 是否需要新增 ADR

- 是 / 否
- 如是：ADR 编号、标题
